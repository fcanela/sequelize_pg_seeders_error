{
  "name": "bugenv",
  "version": "1.0.0",
  "description": "Proof of concept of non-working seeders",
  "main": "index.js",
  "dependencies": {
    "pg": "^4.5.1",
    "sequelize": "^3.19.3",
    "sequelize-cli": "^2.3.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "./node_modules/.bin/sequelize db:migrate",
    "migrate:undo": "./node_modules/.bin/sequelize db:migrate:undo",
    "seed": "./node_modules/.bin/sequelize db:seed:all",
    "start": "npm run migrate && npm run seed"
  },
  "author": "Francisco Canela Gonzalez <fcanela.dev+npm@gmail.com>",
  "license": "ISC"
}
